
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <header>
    <h1>Cinematrix</h1>
    <ul>
      <% if @username %>
        <form action="/logout" method="post">
          <li><button type="submit">Logout</button></li>
        </form>
      <% else %>
        <li><button><a href="/login">login</a></button></li>
        <li><button><a href="/register">register</a></button></li>
      <% end %>
      <% if @username %>
      <li>user: <%= h(@username)%></li>
      <% end %>

    </ul>

  </header>
<div class="container">
<% @data.each do |movie| %>
  <a href="/movies/<%= h(movie['id']) %>">
    <div class="card">
      <div class="image"><img class="image" src="<%= h(movie['image']) %>"></div>
      <div class="title"><%= h(movie['title']) %></div>
      <div class="geanera"><%= h(movie['geanera']) %></div>
    </div>
  </a>
<% end %> 
<% if @username %>
<a href="/movies/new" class="button"><button>New movie</button></a>
<% end %>
</div>

</body>
</html>